<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_user_select_activty"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"

    tools:context="test.sks.com.busapp.UserSelectActivty">
    <!--android:paddingBottom="@dimen/activity_vertical_margin"-->
    <!--android:paddingLeft="@dimen/activity_horizontal_margin"-->
    <!--android:paddingRight="@dimen/activity_horizontal_margin"-->
    <!--android:paddingTop="@dimen/activity_vertical_margin"-->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">
        <Button
            android:id="@+id/btn_passenger"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:text="승객"
            android:background="#DAD9FF"
            android:onClick="btnSelectPassenger"
            android:layout_weight="1"
            android:textColor="@color/colorPrimaryText"
            android:contentDescription="승객 모드로 시작하기"
            android:textSize="100sp"/>
        <Button
            android:id="@+id/btn_driver"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="#FAF4C0"
            android:text="운전자"
            android:textColor="@color/colorPrimaryText"
            android:contentDescription="운전자 모드로 시작하기"
            android:textSize="100sp"
            android:onClick="btnSelectDriver"
            android:layout_weight="1"/>
    </LinearLayout>
</RelativeLayout>
